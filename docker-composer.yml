version: '3.3'

services:
    web:
        image: node:10


   db:
     image: mysql:8.0
     volumes:
       - db_data:/var/lib/mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: test
       MYSQL_DATABASE: blog
       MYSQL_USER: root
       MYSQL_PASS: test
